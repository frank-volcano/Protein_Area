name: Protein_Area-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # MDAnalysis
  - MDAnalysis

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  # other dependency
  - scipy
  - shapely

  # Pip-only installs
  #- pip:
  #  - codecov

